<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- External Storage - Scoped Storage for Android 11+ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    
    <!-- Device Features - Optional -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    
    <!-- Window Management -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />
    <uses-permission android:name="android.permission.ACTION_MANAGE_OVERLAY_PERMISSION" />
    
    <!-- Package Installation (Android 12+) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    
    <!-- Query Package Visibility (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    
    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MANIFEST" />
    
    <!-- Power Management -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Process Control -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    
    <!-- VPN Service -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />
    
    <!-- Boot Completion -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Superuser (requires rooted device) -->
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />

    <application
        android:name=".BoxApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Base.AppTheme"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".MAct"
            android:configChanges="screenSize|orientation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".LogAct"
            android:configChanges="screenSize|orientation"
            android:exported="false" />

        <activity 
            android:name=".ui.AppInstallerActivity"
            android:configChanges="screenSize|orientation"
            android:exported="false" />
        
        <!-- External Authentication Service -->
        <service
            android:name=".auth.ExternalAuthService"
            android:exported="false"
            android:foregroundServiceType="manifest" />
        
        <!-- Login Settings Activity -->
        <activity
            android:name=".ui.LoginSettingsActivity"
            android:exported="false"
            android:configChanges="screenSize|orientation" />

    </application>

</manifest>
